# QuranDesk environment configuration.
# Copy this file to `.env` and set the catalog URL.

# Full URL to the catalog manifest. The main process fetches and caches it.
MAIN_VITE_MANIFEST_URL=https://your-bucket.r2.dev/reciters.json
